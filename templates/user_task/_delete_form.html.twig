<form method="post" action="{{ path('user_task_delete', {'id': user_task.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_task.id) }}">
    <button class="btn btn-outline-danger">Delete</button>
</form>
