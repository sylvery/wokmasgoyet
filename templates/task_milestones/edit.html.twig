{% extends 'base.html.twig' %}

{% block title %}Update Milestone {{ parent() }}{% endblock %}

{% block body %}
    {% include "main/page_header.html.twig" with {
        page_title: 'Update Task Milestone Details',
        actions: [
            {
                action_route: 'user_task_show',
                action_class: 'warning',
                link_class: 'dark',
                label: 'back to parent task',
                link_options: { id: task_milestone.task.id },
            },{
                action_route: 'home',
                label: 'back to home',
            },
        ]
    } %}
    {{ include('task_milestones/_form.html.twig', {'button_label': 'Update'}) }}
    {% if is_granted('ROLE_ADMIN') %}
        {{ include('task_milestones/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
